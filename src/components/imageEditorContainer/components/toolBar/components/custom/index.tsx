import React, { useRef, useState, useEffect } from 'react';
import { Tabs } from 'antd-mobile';

import './index.scss';

interface IProps {}

const Custom = ({}: IProps) => {
  return <div className="custom">22</div>;
};

export default Custom;
